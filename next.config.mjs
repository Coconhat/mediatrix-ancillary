/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['via.placeholder.com'], // Add placeholder.com to the list of allowed image domains
    },
  };

export default nextConfig;
